---
import PluginDataFile from '../../../plugin-data.json' assert { type: 'json' };
import { getRetiredPluginsPerMonth } from '../utils/utils';
import PluginPerMonthChart from './svelte/pluginPerMonthChart.svelte';

const dataPoints = getRetiredPluginsPerMonth(PluginDataFile);
---

<PluginPerMonthChart dataPoints={dataPoints} title="Newly Retired Plugins Per Month" type="bar" client:only="svelte" />

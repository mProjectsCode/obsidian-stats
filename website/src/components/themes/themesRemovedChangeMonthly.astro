---
import ThemeDataFile from '../../../../theme-data.json' assert { type: 'json' };
import { getThemeCountRemovedChangeMonthly } from '../../../../src/theme/data';
import DataPerMonthChart from '../svelte/genericCharts/dataPerMonthChart.svelte';

const dataPoints = getThemeCountRemovedChangeMonthly(ThemeDataFile);
---

<DataPerMonthChart dataPoints={dataPoints} title="Newly Retired Themes Per Month" type="bar" client:only="svelte" />

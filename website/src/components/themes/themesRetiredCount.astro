---
import ThemeDataFile from '../../../../theme-data.json' assert { type: 'json' };
import { getRetiredThemeCountPerMonth } from '../../../../src/theme/data';
import DataPerMonthChart from '../svelte/dataPerMonthChart.svelte';

const dataPoints = getRetiredThemeCountPerMonth(ThemeDataFile);
---

<DataPerMonthChart dataPoints={dataPoints} title="Total Retired Theme Count" type="line" client:only="svelte" />

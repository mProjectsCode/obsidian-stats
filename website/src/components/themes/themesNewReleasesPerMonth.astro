---
import ThemeDataFile from '../../../../theme-data.json' assert { type: 'json' };
import {getNewThemeReleasesPerMonth} from '../../utils/utils';
import DataPerMonthChart from '../svelte/dataPerMonthChart.svelte';

const dataPoints = getNewThemeReleasesPerMonth(ThemeDataFile);
---

<DataPerMonthChart dataPoints={dataPoints} title="New Theme Releases per Month" client:only="svelte" />

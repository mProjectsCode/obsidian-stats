---
import ThemeDataFile from '../../../../theme-data.json' assert { type: 'json' };
import { getThemeCountAddedMonthly } from '../../../../src/theme/data';
import DataPerMonthChart from '../svelte/genericCharts/dataPerMonthChart.svelte';

const dataPoints = getThemeCountAddedMonthly(ThemeDataFile);
---

<DataPerMonthChart dataPoints={dataPoints} title="New Theme Releases per Month" client:only="svelte" />

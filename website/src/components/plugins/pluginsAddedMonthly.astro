---
import { getPluginCountAddedMonthly } from '../../../../src/plugin/data';
import { getPluginData } from '../../../../src/utils';
import DataPerMonthChart from '../svelte/genericCharts/dataPerMonthChart.svelte';

const dataPoints = getPluginCountAddedMonthly(getPluginData());
---

<DataPerMonthChart dataPoints={dataPoints} title="New Plugin Releases per Month" client:only="svelte" />
